# Domain notes as raw inputs

The existing `/mobu` tree is currently treated as a broad raw/domain-input layer.

Interpretation rules:
- reading notes, work notes, parenting notes, and study notes stay as source material first
- only mature, repeatedly useful knowledge should be promoted into `/wiki`
- colocated images should remain in place until attachment migration rules are stabilized

Primary source tree:
- `../../mobu/`
